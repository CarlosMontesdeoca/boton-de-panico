import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'boton-de-banico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
